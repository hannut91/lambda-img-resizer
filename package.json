{
  "name": "resizer",
  "version": "1.0.0",
  "description": "image-resizer using s3, lambda, api-gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.112.0",
    "sharp": "^0.18.2"
  }
}
